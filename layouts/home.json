{{ define "main" }}
"title": {{ .Title  | jsonify }},
"pages": [{{ range $k, $v := site.RegularPages }}
  {{ if $k }},{{ end }}
  {
    "link": {{ .RelPermalink | jsonify }},
    "title": {{ .LinkTitle | jsonify }}
  }{{ end }}
]
{{ end }}
